# tm init installs the PreToolUse hook when .claude/ is present.
mkdir .claude
exec tm init
stdout 'Installed PreToolUse hook'

exec cat .claude/settings.json
stdout 'tm check-action --hook'

# Running tm init a second time should report the hook is already present.
exec tm init
stdout 'already'
